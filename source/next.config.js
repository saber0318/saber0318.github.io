/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // assetPrefix: 'https://saber0318.github.io/',
  // basePath: '/',
  // distDir: '',
}

module.exports = nextConfig
